extends layout

include mixins/_loginForm
include mixins/_forgotForm

block content
    .inner
        +loginForm()
        p No #[ account? ]
            strong: a(href="/register") Register!
        +forgotForm()